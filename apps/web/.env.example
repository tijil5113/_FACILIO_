# Leave empty to use the Vite development proxy (/api -> Flask).
VITE_API_BASE_URL=

# Used only by the Vite proxy. Override in Docker Compose to reach the API service.
VITE_PROXY_TARGET=http://127.0.0.1:5050
